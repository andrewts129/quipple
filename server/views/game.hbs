<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>{{game.id}} - Quipple</title>
        <script defer src="https://cdn.jsdelivr.net/npm/socket.io-client@2/dist/socket.io.js"></script>
        <script defer src="/game.js"></script>
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
        <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
        <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
        <link rel="manifest" href="/site.webmanifest">
    </head>
    <body>
        <h1>Quipple</h1>
        <h2>Game ID: {{game.id}}</h2>
        <h3>Current Players</h3>
        <ul id="playerList" data-this-player-id="{{player.id}}" data-this-player-name="{{player.screenName}}">
            <li>{{game.owner.screenName}}</li>
            {{#each game.players}}
                <li>{{this.screenName}}</li>
            {{/each}}
        </ul>
        <div class="lobbyStage">
            <button id="startGameButton" type="button" style="display: {{startButtonDisplay}};">Start Game</button>
            <p id="startWaitingMessage" style="display: {{waitingMessageDisplay}};">
                Waiting for <span id="ownerName">{{game.owner.screenName}}</span> to start the game...
            </p>
            <p id="countdownMessage"></p>
        </div>
        <div class="questionStage" style="display: none;">
            <h3>Question</h3>
            <p id="question">Loading...</p>
            <h3>Your Answer</h3>
            <div class="questionStage-presubmit">
                <form>
                    {{! TODO add label }}
                    <input id="answerTextInput" name="answerText" type="text" minlength="1" maxlength="128" spellcheck="true">
                    <button id="submitAnswerButton" type="button">Submit</button>
                </form>
            </div>
            <div class="questionStage-postsubmit" style="display: none;">
                <p id="mySubmission"></p>
                <p id="postsubmitMessage">Waiting for other players to submit...</p>
            </div>
        </div>
    </body>
</html>
